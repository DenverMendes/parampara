import { CustodianDashboard } from '@/components/custodian-dashboard';

export const metadata = {
  title: 'Custodian dashboard — Parampara',
  description: 'A calm, human dashboard for cultural custodians.',
};

export default function DashboardPage() {
  return <CustodianDashboard />;
}
